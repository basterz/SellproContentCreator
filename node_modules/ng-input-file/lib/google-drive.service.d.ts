import { OnDestroy } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
export interface GoogleApiConfig {
    developerKey: string;
    appId: string;
    clientId: string;
}
export declare class GoogleDriveService implements OnDestroy {
    private http;
    isDevMode: boolean;
    config: GoogleApiConfig;
    private file$;
    selectedFile: Observable<File>;
    pickerApiLoaded: boolean;
    oauthToken: any;
    googleApiUrl: string;
    googleApiLoaded: boolean;
    setDevMode(): void;
    log: (s?: any, ...optional: any[]) => void;
    constructor(http: HttpClient);
    ngOnDestroy(): void;
    init(config: GoogleApiConfig): void;
    loadScript(): Promise<any>;
    loadPicker(): void;
    private doLoadPicker;
    private onAuthApiLoad;
    private onPickerApiLoad;
    private handleAuthResult;
    private createPicker;
    private pickerCallback;
    private getFile;
}
